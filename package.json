{
  "name": "budgie",
  "version": "1.0.0",
  "description": "My budget program for myself",
  "engines": {
    "node": "10.15.0",
    "yarn": "1.13.0"
  },
  "scripts": {
    "start": "node ./server/index.js",
    "dev": "concurrently \"yarn --cwd server run dev\" \"yarn --cwd client start\"",
    "heroku-postbuild": "YARN_PRODUCTION=false yarn --cwd client install && yarn --cwd run build",
    "postinstall": "yarn --cwd server install && yarn --cwd client install"
  },
  "author": "Zac Braddy",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}
